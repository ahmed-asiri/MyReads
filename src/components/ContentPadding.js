import React from 'react'

function ContentPadding({ children }) {
    return (
        <div className="content-padding">
            { children }
        </div>
    )
}

export default ContentPadding
